mkdir -p $HOME/tmp
TXT=$HOME/tmp/todo.txt
GPG=$HOME/writing-git/todo.txt.gpg
if [[ ~/LSUOneDrive/todo.txt.gpg -nt /tmp/todo.txt ]]
then
    touch $TXT
    cp $TXT{,.sav} 
    gpg -o $TXT -d $GPG
    vimdiff $TXT{,.sav}
else
    echo already decrypted
fi
